name: Verify canonical rich Claude golden

on:
  push:
    paths:
      - 'tests/fixtures/claude/claude-rich-subagent.jsonl'
      - 'tests/golden/claude/**'
      - 'tests/claude-canonical-golden-manifest.json'
      - 'tests/validate-claude-canonical-golden.py'
      - '.github/workflows/verify-claude-canonical-final.yml'

permissions:
  contents: write

jobs:
  verify:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Run core tests
        run: npm test
      - name: Run Phase 2 integrity gate
        run: python tests/validate-phase2-baseline.py
      - name: Validate canonical ChatGPT golden
        run: python tests/validate-canonical-golden.py
      - name: Validate canonical Claude golden
        run: python tests/validate-claude-canonical-golden.py
      - name: Remove temporary workflow
        env:
          GH_TOKEN: ${{ github.token }}
        run: |
          git rm .github/workflows/verify-claude-canonical-final.yml
          git config user.name github-actions
          git config user.email github-actions@github.com
          git commit -m 'chore: remove temporary Claude canonical verification workflow'
          git push
