# Existing implementation comparison

The pre-Core implementation audit is split into focused documents so the evidence remains editable and each maintained document stays below the repository line limit.

- `docs/EXISTING-IMPLEMENTATIONS-BASELINES.md` — reviewed baselines, shared semantics, application boundaries, provider differences, and the first AgentPanelSpeaker design finding.
- `docs/EXISTING-IMPLEMENTATIONS-IMPLICATIONS.md` — remaining AgentPanelSpeaker findings, duplication/drift risks, useful existing abstractions, target ownership, testing implications, and migration consequences.

These documents remain required design input. They are not historical notes to ignore after initial development.

For the current architecture/file map, see `DESIGN.md`. For durable decisions, see `DECISIONS.md`. For migration verification requirements, see `TESTING.md`.
